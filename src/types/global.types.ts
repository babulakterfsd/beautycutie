export interface NTypes {}

export type Message = {
  username: string;
  email: string;
  message: string;
};

export type BlogPostType = {
  title: string;
  image: string;
  content: string;
  category: string;
};
